<div class="post-index" >
  <div class="admin-head col-md-12">
    <div class="p-4"></div>
      <div class="head-title p-5">
        <h1>Dojo Forum</h1>          
      </div>
  </div>
</div>
<div class="container">
  <div class="row">

    <ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
      <li class="nav-item"><%= link_to "All", root_path, class: "nav-link" %></li>
        <% @categories.each do |category| %>
          <li class="nav-item"><%= link_to category.name, category_path(category), class: "nav-link #{'active' if category == @category}" %></li>
        <% end %>
    </ul>  
    <br>

    <%= render partial: "shared/post_list" %>

  </div>
</div>